# The leaky-app fixture and the unit tests contain deliberately fake keys so the scanner has
# something to catch. They are not real credentials. Exclude them from GitHub secret scanning
# so forks do not get "secrets detected" emails.
paths-ignore:
  - "examples/leaky-app/**"
  - "tests/**"
